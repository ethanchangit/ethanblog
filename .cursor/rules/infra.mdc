---
description: "用户消息以 /infra 或 基建： 开头，或要求修改组件、样式、布局、API、测试、部署时适用。遵循 AGENTS.md，不创建或改写 story 正文。"
globs:
  - src/components/**
  - src/lib/**
  - src/pages/**
  - plugins/**
alwaysApply: false
---

# `/infra` — 网站基建模式

路由表见 [AGENTS.md](../../AGENTS.md#任务路由)。本站是 **Ethan Chang 的个人博客**（https://ethanchang.io）。

## 收到 `/infra` 后

1. **只动基建层**：`src/components/`、`src/lib/`、`src/pages/`、`plugins/`、CI 配置等。
2. **禁止内容创作**：不创建或改写 `src/content/` 下的 MDX，除非用户明确要求。
3. **遵循约束**：[AGENTS.md](../../AGENTS.md) + [media/README.md](../../src/components/media/README.md)。
4. **新组件准入**：满足 media/README 契约（含视觉极简）→ `/lab` 点亮 → 补测试 → 更新 README。
5. **验证四连**：`npm run validate:content && npm run check && npm run build && npm run test`。
6. **后台**：本地 `npm run dev` 的 http://localhost:4321/dashboard 免密码，只在本机 dev。生产 https://ethanchang.io/dashboard 仍要密码。`/studio` 不是写作入口，也不进生产。写作在 Heptabase。push 到 `main` 在验证通过后自动部署。
7. **视觉**：公开站点不引入边框、背景色块、圆角卡片、色条等装饰（`/dashboard` 为可读性例外；文章/项目页眉的 `.article-meta` 元数据块也是例外）；遵循 [AGENTS.md](../../AGENTS.md) 样式约束。中文 `.i18n-zh` 里 `<a>` 紧贴汉字，不要因换行挤出空格。
