---
import { formatDate } from '@/lib/format';

interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<span class="i18n-zh" aria-hidden="true">{formatDate(date, 'zh-CN')}</span><span class="i18n-en"
  >{formatDate(date, 'en')}</span
>
