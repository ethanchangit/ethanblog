---
import { formatDate } from '@/lib/format';

interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<span class="i18n-zh">{formatDate(date, 'zh-CN')}</span><span class="i18n-en" aria-hidden="true"
  >{formatDate(date, 'en')}</span
>
