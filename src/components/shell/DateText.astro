---
import { formatDate } from '@/lib/format';

interface Props {
  date: Date;
}

const { date } = Astro.props;
const lang = Astro.locals.lang === 'en' ? 'en' : 'zh-CN';
---

<span class={lang === 'en' ? 'i18n-en' : 'i18n-zh'}>{formatDate(date, lang)}</span>
