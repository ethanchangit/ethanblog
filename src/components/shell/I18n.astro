---
import { t, type CopyKey } from '@/lib/i18n';

interface Props {
  k: CopyKey;
}

const { k } = Astro.props;
const lang = Astro.locals.lang === 'en' ? 'en' : 'zh-CN';
---

<span class={lang === 'en' ? 'i18n-en' : 'i18n-zh'}>{t(lang, k)}</span>
