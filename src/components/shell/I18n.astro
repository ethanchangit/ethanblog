---
import { t, type CopyKey } from '@/lib/i18n';

interface Props {
  k: CopyKey;
}

const { k } = Astro.props;
---

<span class="i18n-zh" aria-hidden="true">{t('zh-CN', k)}</span><span class="i18n-en">{t('en', k)}</span>
